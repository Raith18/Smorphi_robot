<?xml version="1.0"?>
<package format="2">
  <name>localization</name>
  <version>0.1.0</version>
  <description>
    ICP-based localization node (no EKF): builds a point-cloud map from the
    LiDAR odometry stream, then registers every new scan against the map with
    point-to-plane ICP. Publishes /localization_pose, the dynamic map->odom
    TF and the map cloud. The interface is FAST-LIO-compatible.
  </description>
  <maintainer email="186810016+Raith18@users.noreply.github.com">Raith18</maintainer>
  <license>MIT</license>

  <buildtool_depend>catkin</buildtool_depend>
  <depend>rospy</depend>
  <depend>std_msgs</depend>
  <depend>sensor_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>nav_msgs</depend>
  <depend>tf2_ros</depend>
  <depend>diagnostic_msgs</depend>
  <depend>message_filters</depend>
  <depend>dataset_loader</depend>
  <depend>lidar_odometry</depend>
  <depend>lidar_processing</depend>
  <exec_depend>python3-numpy</exec_depend>
  <exec_depend>python3-scipy</exec_depend>
</package>
