<?xml version="1.0"?>
<package format="2">
  <name>time_sync</name>
  <version>0.1.0</version>
  <description>
    Time synchronization node: uses message_filters (exact or approximate
    synchronizer) to group camera / LiDAR / IMU / GPS messages that describe
    the same instant, republishes them on /time_sync/* topics and emits
    diagnostic statistics. This is the gatekeeper that makes camera-lidar
    fusion (Phase 3) and multi-sensor localization (Phase 5) possible.
  </description>
  <maintainer email="186810016+Raith18@users.noreply.github.com">Raith18</maintainer>
  <license>MIT</license>

  <buildtool_depend>catkin</buildtool_depend>
  <depend>rospy</depend>
  <depend>std_msgs</depend>
  <depend>sensor_msgs</depend>
  <depend>diagnostic_msgs</depend>
  <depend>message_filters</depend>
</package>
