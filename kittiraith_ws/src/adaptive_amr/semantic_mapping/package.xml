<?xml version="1.0"?>
<package format="2">
  <name>semantic_mapping</name>
  <version>0.1.0</version>
  <description>
    Semantic mapping node: accumulates the semantic-colored LiDAR cloud
    (/semantic_map/colored_points) into a global voxel map in the map frame,
    using the localization/odometry pose. Publishes /semantic_map (PointCloud2
    with rgb) and statistics. CPU-friendly pure NumPy.
  </description>
  <maintainer email="186810016+Raith18@users.noreply.github.com">Raith18</maintainer>
  <license>MIT</license>

  <buildtool_depend>catkin</buildtool_depend>
  <depend>rospy</depend>
  <depend>std_msgs</depend>
  <depend>sensor_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>nav_msgs</depend>
  <depend>tf2_ros</depend>
  <depend>diagnostic_msgs</depend>
  <depend>message_filters</depend>
  <depend>dataset_loader</depend>
  <depend>lidar_processing</depend>
  <exec_depend>python3-numpy</exec_depend>
</package>
